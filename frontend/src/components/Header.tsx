export default function HomeHeader() {
  return (
    <header
      style={{
        background: "linear-gradient(180deg, #32190F, #1f0f08)",
        padding: "20px 32px",
        color: "#fff",
        fontSize: 20,
        fontWeight: 600,
      }}
    >
      Promis Odontologia
    </header>
  );
}
